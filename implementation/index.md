# implementation/, the how

The rules, the playbook, and the default MOS for standing up a Manence project.

- [Spec.md](Spec.md), the concrete rules (OKF, naming, freshness, security).
- [Implementation.md](Implementation.md), the playbook (skeleton, wiki method, connecting an adapter).
- [mos/](mos/BOOTSTRAP.md), the default MOS, ready to copy: identity files, the 6 base skills, hooks, and `BOOTSTRAP.md`, the one-time startup ritual (single source, L2). The French set ships in `mos/fr/`.
- [example/](example/index.md), a small running "Lumo" knowledge base.
